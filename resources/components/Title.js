import { h } from 'preact';

const Title = () => (
  <div class="text-2xl font-bold">
    <strong>HackerNews Minimal</strong>
    <small class="text-sm text-gray-600">by Reflux</small>
  </div>
);

export default Title;
